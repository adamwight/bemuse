# @configure_input@
DEST = @prefix@

all: install
#test

clean build install::
	$(MAKE) -C cataloger $@
	$(MAKE) -C cli $@
#	[ -f desktop/Makefile ] && $(MAKE) -C desktop $@

#TODO
install:: build
	$(MAKE) -C cli $@
	mkdir -p $(DEST)/web/doc/
	chmod u+w $(DEST)/web/doc/
	cp -R doc/* $(DEST)/web/doc/
	chmod a-w $(DEST)/web/doc/

distclean: clean
	rm -f config.* Makefile */Makefile
	rm -rf autom4te.cache
	[ -d releases ] && echo "What will you do with releases/ ?"

maintainer-clean: distclean
	rm -f configure
